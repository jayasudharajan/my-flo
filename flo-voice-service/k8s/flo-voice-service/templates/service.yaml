---
apiVersion: v1
kind: Service
metadata:
  name: {{ template "flo-voice-service.fullname" . }}
  labels:
    app: {{ template "flo-voice-service.name" . }}
    app.kubernetes.io/name: {{ include "flo-voice-service.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    helm.sh/chart: {{ include "flo-voice-service.chart" . }}
{{- with .Values.service.annotations }}
  annotations:
{{ toYaml . | indent 4 }}
{{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "flo-voice-service.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
