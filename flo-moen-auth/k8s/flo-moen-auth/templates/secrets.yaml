apiVersion: v1
kind: Secret
metadata:
  name: flo-moen-auth-secrets
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ template "flo-moen-auth.name" . }}
    chart: {{ template "flo-moen-auth.chart" . }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
type: Opaque
data:
  {{- range $key, $value := .Values.secrets.datas }}
    {{ $key }}: {{ default "" $value | b64enc | quote }}
  {{- end }}
