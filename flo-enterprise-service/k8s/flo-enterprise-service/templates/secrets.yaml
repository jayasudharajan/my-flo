apiVersion: v1
kind: Secret
metadata:
  name: flo-enterprise-service-secrets
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ template "flo-enterprise-service.name" . }}
    chart: {{ template "flo-enterprise-service.chart" . }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
type: Opaque
data:
  {{- range $key, $value := .Values.secrets.datas }}
    {{ $key }}: {{ default "" $value | b64enc | quote }}
  {{- end }}
