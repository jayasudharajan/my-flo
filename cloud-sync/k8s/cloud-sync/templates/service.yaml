{{- $servicePorts := .Values.servicePorts -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloud-sync.fullname" . }}
  labels:
    app: {{ include "cloud-sync.name" . }}
    app.kubernetes.io/name: {{ include "cloud-sync.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    helm.sh/chart: {{ include "cloud-sync.chart" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: {{ .Values.service.port }}
  selector:
    app.kubernetes.io/name: {{ include "cloud-sync.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
