kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ include "openresty-config-dir" . | quote }}
  labels:
    app: "openresty"
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
data:
{{ (.Files.Glob "config/openresty/*").AsConfig | indent 2 }}
