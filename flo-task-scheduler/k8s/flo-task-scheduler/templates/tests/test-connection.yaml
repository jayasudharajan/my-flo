apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flo-task-scheduler.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "flo-task-scheduler.name" . }}
    helm.sh/chart: {{ include "flo-task-scheduler.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "flo-task-scheduler.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
