apiVersion: v1
kind: Secret
metadata:
  name: flo-task-scheduler-secrets
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ template "flo-task-scheduler.name" . }}
    chart: {{ template "flo-task-scheduler.chart" . }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
type: Opaque
data:
  {{- range $key, $value := .Values.secrets.datas }}
    {{ $key }}: {{ default "" $value | b64enc | quote }}
  {{- end }}
