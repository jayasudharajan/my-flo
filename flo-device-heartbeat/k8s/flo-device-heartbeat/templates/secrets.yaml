apiVersion: v1
kind: Secret
metadata:
  name: flo-device-heartbeat-secrets
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ template "flo-device-heartbeat.name" . }}
    chart: {{ template "flo-device-heartbeat.chart" . }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
type: Opaque
data:
  {{- range $key, $value := .Values.secrets.datas }}
    {{ $key }}: {{ default "" $value | b64enc | quote }}
  {{- end }}
