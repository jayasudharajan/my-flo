---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: "{{ include "flo-device-heartbeat.fullname" . }}-restarter"
rules:
  - apiGroups: ["apps", "extensions"]
    resources: ["deployments"]
    resourceNames: ["{{ include "flo-device-heartbeat.fullname" . }}"]
    verbs: ["get", "patch", "list", "watch"]
