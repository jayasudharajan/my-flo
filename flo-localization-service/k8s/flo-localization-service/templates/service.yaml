apiVersion: v1
kind: Service
metadata:
  name: {{ include "flo-localization-service.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "flo-localization-service.name" . }}
    helm.sh/chart: {{ include "flo-localization-service.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "flo-localization-service.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
