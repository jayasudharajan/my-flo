apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flo-localization-service.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "flo-localization-service.name" . }}
    helm.sh/chart: {{ include "flo-localization-service.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "flo-localization-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
