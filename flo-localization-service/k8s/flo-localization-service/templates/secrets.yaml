apiVersion: v1
kind: Secret
metadata:
  name: flo-localization-service-secrets
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ template "flo-localization-service.name" . }}
    chart: {{ template "flo-localization-service.chart" . }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
type: Opaque
data:
  {{- range $key, $value := .Values.secrets.datas }}
    {{ $key }}: {{ default "" $value | replace ":" "" | b64enc | quote }}
  {{- end }}
