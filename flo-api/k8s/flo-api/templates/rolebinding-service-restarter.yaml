---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: "{{ include "flo-api.fullname" . }}-restarter"
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: "{{ include "flo-api.fullname" . }}-restarter"
subjects:
  - kind: ServiceAccount
    name: "{{ include "flo-api.fullname" . }}-restarter"
