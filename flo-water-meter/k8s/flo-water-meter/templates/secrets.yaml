apiVersion: v1
kind: Secret
metadata:
  name: flo-water-meter-secrets
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ template "flo-water-meter.name" . }}
    chart: {{ template "flo-water-meter.chart" . }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
type: Opaque
data:
  {{- range $key, $value := .Values.secrets.datas }}
    {{ $key }}: {{ default "" $value | b64enc | quote }}
  {{- end }}
